# Voice Profile — `<account handle>` (`<platform>`)

Fill-in template documenting the shape of an Antwork voice analysis. This is what
`save_voice_analysis(account_id, analysis)` persists. One profile **per account** —
a personal LinkedIn voice is not the same as a company X voice.

> Built from `<N>` posts · Last synced `<date>` · Status: `completed`

---

## tone
One or two words, named — not hedged. e.g. `founder-mode, contrarian`
(avoid useless labels like "professional and engaging").

## voice / pronouns
- Person: `first-person singular ("I/my")` | `first-person plural ("we/our")`
- On whose behalf: `solo creator` | `brand/team`

## style
- Sentence length: `short and punchy` | `medium` | `long narrative`
- Paragraph rhythm: `one-line paragraphs` | `2–3 sentence blocks`
- Structure habits: `uses lists` / `uses line breaks for emphasis` / `single-block`

## emoji_policy
- Frequency: `none` | `sparing` | `signature`
- Specific emoji actually used: `<list, or "none">`

## hashtag_policy
- Count per post: `<n>`
- Placement: `inline` | `footer block` | `none`
- Branded/recurring tags: `<list>`

## cta_patterns
How posts close: `open question` | `soft ask` | `hard CTA` | `link drop` | `no CTA`
Example closers actually used:
- `<quote>`
- `<quote>`

## example_phrases
Recurring openers, signature words, and turns of phrase that make it *them*
(quote real ones from their posts):
- `<phrase>`
- `<phrase>`
- `<phrase>`

## mannerisms
Distinctive habits worth preserving:
- `<e.g. always opens with a blunt one-line claim>`
- `<e.g. ends threads with a numbered recap>`

## do
- `<thing that makes drafts sound like them>`
- `<thing>`

## dont
Explicit anti-patterns — including the universal AI tells:
- Never use: "Here's the thing:", "Let me break it down", "Buckle up", "In today's fast-paced world"
- No emoji-heavy openers
- `<account-specific don't>`
